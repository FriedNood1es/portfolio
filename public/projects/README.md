# Project screenshots

Drop real screenshots here (16:10 aspect, e.g. 1600×1000, as `.png` or `.webp`),
then set the `image` field on the matching project in `lib/content.ts`, e.g.:

```ts
image: "/projects/bastafda.png",
```

Until then, each project card renders a styled placeholder panel.
